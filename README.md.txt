techonolgies used : mern stack To run the application-> 1- cd client 2- npm i 3- npm start this command hepls you to run the client on localhost:3000 and heroku server communication.

to run the server locally-> 1- npm i 2- npm run dev this will run the server on the localhost:5000

If you aslo want to run the client with the local server, then you first have to change the api url with http://localhost:5000 then run the client and test your web application.